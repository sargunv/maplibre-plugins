{
  "name": "@maplibre-plugins/paint",
  "version": "0.1.0",
  "description": "Paint property evaluation and transitions shared by the maplibre-gl-js plugin layers: style-spec expressions, CSS colors, and MapLibre Native style transitions.",
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": { "types": "./src/index.ts", "default": "./src/index.ts" }
  },
  "files": ["dist"],
  "scripts": { "build": "vp pack", "test": "vp test run", "check": "vp check" },
  "dependencies": { "@maplibre/maplibre-gl-style-spec": "catalog:" },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "publishConfig": {
    "exports": {
      ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
    }
  }
}

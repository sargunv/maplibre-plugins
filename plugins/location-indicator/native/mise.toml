[tools]
"core:zig" = "{{vars.zig_version}}"

[tasks.build]
description = "Build the location-puck plugin shared library into zig-out/lib."
run = "zig build"

[tasks.test]
description = "Run the plugin's Zig unit tests (no native library needed)."
run = "zig build test"
